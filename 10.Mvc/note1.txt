controller -> model -> view

controller model ko manupulate karta hai aur model view ko update karta hai. 
===============================================================================================================
🧠 What is MVC?

Model - Handle data and business logic (usually connected to a database like MongoDb).
View - responsible for presentation. In mern react used for presentation. 
controller - handle user requrest, interact with model, and sends back apporiate response.Handles logic like create, read, update, delete (CRUD):
 
Model (MongoDB + Mongoose)
-Controller (Express route logic)
-(Optionally View) — in APIs, you don't render HTML, you return JSON.


🧩 MVC in a MERN Backend (Node.js + Express + MongoDB)
🔸 Folder Structure Example:
backend/
│
├── controllers/
│   └── studentController.js     # All logic like createStudent, getStudent etc.
│
├── models/
│   └── studentModel.js          # Mongoose Schema
│
├── routes/
│   └── studentRoutes.js         # Express routes
│
├── config/
│   └── db.js                    # MongoDB connection
│
├── app.js                       # Express setup

==========================================================================================================