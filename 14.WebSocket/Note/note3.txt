Broadcasting - now we will emit the event from server to user. 
In order to send an event to everyone, Socket.IO gives us the io.emit() method.

// this will emit the event to all connected sockets
io.emit('hello', 'world'); 