Data Modeling
-------------

moongoes.model("kya model banau",kiske base pr model banau);

moongoes - Mongoose is an ODM (Object Data Modeling) library for MongoDB and Node.js. It helps you:
Define schemas
Create models
Perform validation

✅ 1. Setup
Install Mongoose in your Node.js project:
npm install mongoose

2. create a connection to MongoDB:


✅ 2. Define a Schema and Model

import mongoose from "moongoose";

//ye jo Schema me jo object hai usme hum data rakhtey hai.
//const userSchema = new mongoose.Schema({});
 
// const userSchema = new mongoose.Schmea({
//   username:String
// })

                //or  more better way

const userSchema = new mongoose.Schema({
username:{
  type:String,
  required: true,
  unique: true,
  lowercase:true,
},
email:{
  type:String,
  required: true,
  unique:true,
},
password:{
  type:String,
  required:[true,"password must "],
}
},{timestamps:true});
//agar password nhi hai tho ek custom message v show kar saktey hai "password must "
//timestamps true matlab ye v store hoga ki data kb create hua and kb update hua. 

//model define kr deye
export const User = mongoose.model("User",userSchema);

//ye jo User hai database me jatey hi users ho jata hai. database me lowercase ho jata hai + pural ho jata hai

example 2
----------

import mongoose from 'mongoose';

const todoSchema = new mongoose.Schema({
  content:{
    type: String,
    required: true,
  },
  complete:{
    type:Boolean,
    default: false,
  },
  createdBy:{
    type:mongoose.Schema.Types.ObjectId,
    ref:"User"
  },
  subTodos:[{
    type: Schema.Types.ObjectId,
    ref:'SubTodo'
  }]//Array of SubTodos
},{timestamps:true});

// createdBy:{
//   type:mongoose.Schema.Types.ObjectId,
//   ref:"User"
//}  yha pr hum User model ka reference de rhe hai. yhi sytax hai reference dene ka.

export const Todo = mongoose.model("Todo",todoSchema);

