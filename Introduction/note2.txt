HTTP stands for HyperText Transfer Protocol.

It’s the language that computers use to talk to each other over the web — especially between a client (like your browser or app) and a server (like a website or API).
Example:
When you type a URL like https://google.com in your browser:
Your browser sends an HTTP request to Google's servers.
Google's server sends back an HTTP response — the HTML, images, etc.
Your browser displays the webpage.

🔷 What is an HTTP client?
An HTTP client is a tool or library that sends HTTP requests and receives HTTP responses.
Examples of HTTP clients:
In the browser: the browser itself is the HTTP client.

In JavaScript (Node.js):

fetch — built-in HTTP client in modern JavaScript
axios — a popular third-party HTTP client
http — built-in Node.js module (older)
undici — a modern, fast HTTP client for Node.js

🔄 Request–Response Cycle (Simple Illustration)
[You] → Send HTTP request (GET /page) → [Server]
[Server] → Sends back HTTP response (HTML) → [You]


Fetching data with Node.js
--------------------------
Undici is an HTTP client library that powers the fetch API in Node.js.